const UserSearch = () => {
    return (
        <div className="container">
            <h1>Búsqueda de Personajes de The Office</h1>            
        </div>
    );
};

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<UserSearch />);